name: httprequestapp
description: Application for getting data from HTTP Requests


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  auto_size_text: ^2.1.0
  http: ^0.12.2
  share: ^0.6.4+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true